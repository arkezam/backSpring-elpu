CREATE TABLE `Foto` (
                        `id` bigint PRIMARY KEY AUTO_INCREMENT,
                        `nombre` varchar(80) NOT NULL ,
                        `mimeType` varchar(255) NOT NULL,
                        `dnifk` varchar(8) NOT NULL
);

